<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- فقط INTERNET لازم است. image_picker از Photo Picker سیستم استفاده می‌کند
         و بدون هیچ permission اضافه‌ای کار می‌کند؛ READ_MEDIA_IMAGES /
         READ_EXTERNAL_STORAGE حذف شدند (حریم خصوصی بهتر + ریویو راحت‌تر). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- اعلان محلی سفارش‌ها (اندروید ۱۳ به بالا ران‌تایم اجازه می‌خواهد؛
         درخواستش در NotificationService.init انجام می‌شود) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="قنادی قند"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data android:name="io.flutter.embedding.android.NormalTheme" android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data android:name="flutterEmbedding" android:value="2"/>
    </application>
</manifest>
